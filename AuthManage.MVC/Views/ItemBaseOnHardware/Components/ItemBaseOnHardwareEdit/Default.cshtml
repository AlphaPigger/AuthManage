@model List<AuthManage.Application.DTOModel.MenuDto>

@foreach (var tem in Model)
{
    if (tem.Name == "硬件条目修改")
    {
        <a href="@tem.Address/@ViewBag.ItemBaseOnHardwareID" class="btn btn-info"><i class="glyphicon glyphicon-edit"></i></a>
    }
}
