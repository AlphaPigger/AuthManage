
@{
    Layout = null;
}

@model List<AuthManage.Application.DTOModel.MenuDto>

@foreach (var tem in Model)
{
    if (tem.Name == "项目删除")
    {
        <a href="@tem.Address/@ViewBag.ProjectID" class="btn btn-danger" onclick="return Delete()"><i class="glyphicon glyphicon-trash"></i></a>
    }

}
