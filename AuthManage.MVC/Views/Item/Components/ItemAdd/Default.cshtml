
@{
    Layout = null;
}

@model List<AuthManage.Application.DTOModel.MenuDto>

@foreach (var tem in Model)
{
    if (tem.Name == "条目增加")
    {
        <a href="@tem.Address" class="btn-default"><i class="glyphicon glyphicon-plus"></i></a>
    }

}
